name: low-rank-cl
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.8
  - pytorch=1.11.0
  - torchvision=0.12.0
  - pip
  - pip:
      - six
      - ipdb
      - scipy
      - scikit-learn
      - pyyaml
      - tqdm
      - timm==0.6.7
